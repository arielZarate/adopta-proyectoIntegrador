import React from "react";
import FormPet from "@/components/form/FormPet";
type Props = {};

const page = (props: Props) => {
  return (
    <>
      <FormPet />
    </>
  );
};

export default page;
